# MCP Billing System

Simple Stripe billing for n8n MCP workflows.

## Setup
1. Copy `.env.example` to `.env`
2. Fill in all values
3. Run `npm install`
4. Run `npm start`

## Deployment
Push to GitHub and deploy on Railway.